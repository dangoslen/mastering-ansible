---
- hosts: control
  become: true
  tasks: 
    - name: Install control tools
      apt: name={{item}} state=present update_cache=yes
      with_items:
       - curl
       - wget
       - python-httplib2
